{
  "name": "piramide-eventos",
  "private": true,
  "description": "Landing page Espaço Pirâmide Eventos — produção Vercel",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "bash scripts/build-dark.sh",
    "vercel-build": "npm run build",
    "build:images": "python3 scripts/optimize-images.py",
    "preview": "npm run build && npx --yes wrangler@4 pages dev ./dist"
  }
}
